# Mergedotpkg

Mergedotpkg finds `.pkgnew` and `.pkgsave` files created by a pkg upgrade and
presents the user with an interactive to merge their changes back into the
actual files.

## Installation

```sh
PREFIX=/usr/local make install
```

## Usage

```sh
mergedotpkg
```

## Tests

```
make test
```
